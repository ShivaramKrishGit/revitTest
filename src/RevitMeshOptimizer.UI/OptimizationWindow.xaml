<Window x:Class="RevitMeshOptimizer.UI.OptimizationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Mesh Optimization Settings" Height="450" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <GroupBox Header="Optimization Settings" Grid.Row="0" Margin="0,0,0,10">
            <StackPanel Margin="5">
                <Label Content="Target Reduction (%):"/>
                <Slider x:Name="ReductionSlider" Minimum="0" Maximum="100" Value="50" 
                        TickFrequency="10" IsSnapToTickEnabled="True"/>
                <TextBox Text="{Binding ElementName=ReductionSlider, Path=Value, UpdateSourceTrigger=PropertyChanged}"/>

                <Label Content="Quality Threshold:"/>
                <Slider x:Name="QualitySlider" Minimum="0" Maximum="1" Value="0.5" 
                        TickFrequency="0.1" IsSnapToTickEnabled="True"/>
                <TextBox Text="{Binding ElementName=QualitySlider, Path=Value, UpdateSourceTrigger=PropertyChanged}"/>
            </StackPanel>
        </GroupBox>

        <GroupBox Header="Feature Preservation" Grid.Row="1" Margin="0,0,0,10">
            <StackPanel Margin="5">
                <CheckBox Content="Preserve Sharp Edges" IsChecked="True" Margin="0,5"/>
                <CheckBox Content="Preserve Texture Boundaries" IsChecked="True" Margin="0,5"/>
                <CheckBox Content="Preserve Material Boundaries" IsChecked="True" Margin="0,5"/>
            </StackPanel>
        </GroupBox>

        <GroupBox Header="Statistics" Grid.Row="2" Margin="0,0,0,10">
            <Grid Margin="5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Original Triangle Count:" Grid.Row="0" Grid.Column="0" Margin="0,5"/>
                <TextBlock x:Name="OriginalCountText" Grid.Row="0" Grid.Column="1" Margin="5,5,0,5"/>

                <TextBlock Text="Optimized Triangle Count:" Grid.Row="1" Grid.Column="0" Margin="0,5"/>
                <TextBlock x:Name="OptimizedCountText" Grid.Row="1" Grid.Column="1" Margin="5,5,0,5"/>

                <TextBlock Text="Reduction Achieved:" Grid.Row="2" Grid.Column="0" Margin="0,5"/>
                <TextBlock x:Name="ReductionAchievedText" Grid.Row="2" Grid.Column="1" Margin="5,5,0,5"/>
            </Grid>
        </GroupBox>

        <ProgressBar x:Name="OptimizationProgress" Grid.Row="3" Height="20" Margin="0,10"/>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Preview" Width="80" Margin="0,0,10,0"/>
            <Button Content="Apply" Width="80" Margin="0,0,10,0"/>
            <Button Content="Cancel" Width="80"/>
        </StackPanel>
    </Grid>
</Window>